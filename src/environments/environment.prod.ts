const scheme = '';
const hostname = '';
const port = '';
const host = '';
const apiRoute = '/api';
const apiEndPoint = apiRoute;

export const environment = {
  production: true,
  scheme: scheme,
  port: port,
  host: host,
  apiRoute: apiRoute,
  apiEndPoint: apiEndPoint
};
